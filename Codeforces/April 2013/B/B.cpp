#include <iostream>
#include <fstream>
#include <string>
#include <cmath>
#include <cstdio>
#include <cstdlib>
#include <vector>
#include <algorithm>
#include <set>
#include <map>
#include <time.h>
#include <cassert>
#include <assert.h>

#define DEBUG
#define ASSERT
#define NAME "b"

typedef long long LL;

using namespace std;

int main ()
{

	freopen (NAME".in", "r", stdin);
	freopen (NAME".out", "w", stdout);

	cin >> y >> x;

	if (x == 0)
	{
		if (y == 7 || y == 9 || y == 11 || y == 13 || y == 19 || y == 20 || y == 22 || y == 24 || y == 25)
			cout << 0 << endl;
		else
			cout << 1 << endl;

		return 0;
	}

	if (x == 1)	

	return 0;

}	